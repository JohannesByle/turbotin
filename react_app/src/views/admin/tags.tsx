import { Box } from "@mui/material";
import { DataGrid } from "@mui/x-data-grid";
import React from "react";

const Tags = (): JSX.Element => {
  return <Box sx={{ height: "100%", flex: 1 }}>{/* <DataGrid /> */}</Box>;
};

export default Tags;
